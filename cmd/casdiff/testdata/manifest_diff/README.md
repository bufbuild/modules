## Manifest Diff test structure

This directory is parsed to a single CAS bucket everytime the test runs, as long as it follows the
following structure:

```
.
├── from
│  └── <from files>
└── to
    └── <to files>
```
